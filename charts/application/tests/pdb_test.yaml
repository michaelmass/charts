suite: test pdb
templates:
  - 'pdb.yaml'
tests:
  - it: should fail with not implemented yet
    set:
      pdb:
        enabled: true
    asserts:
      - failedTemplate:
          errorMessage: 'not implemented yet'
