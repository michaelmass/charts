suite: test pvc
templates:
  - 'pvc.yaml'
tests:
  - it: should fail with not implemented yet
    set:
      pvc:
        enabled: true
    asserts:
      - failedTemplate:
          errorMessage: 'not implemented yet'
