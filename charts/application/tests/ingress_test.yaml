suite: test ingress
templates:
  - 'ingress.yaml'
tests:
  - it: should fail with not implemented yet
    set:
      ingress:
        enabled: true
    asserts:
      - failedTemplate:
          errorMessage: 'not implemented yet'
