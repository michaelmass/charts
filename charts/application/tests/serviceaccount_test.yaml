suite: test serviceaccount
templates:
  - 'serviceaccount.yaml'
tests:
  - it: should fail with not implemented yet
    set:
      serviceaccount:
        enabled: true
    asserts:
      - failedTemplate:
          errorMessage: 'not implemented yet'
