suite: test service
templates:
  - 'service.yaml'
tests:
  - it: should fail with not implemented yet
    set:
      service:
        enabled: true
    asserts:
      - failedTemplate:
          errorMessage: 'not implemented yet'
