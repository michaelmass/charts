suite: test vpa
templates:
  - 'vpa.yaml'
tests:
  - it: should fail with not implemented yet
    set:
      vpa:
        enabled: true
    asserts:
      - failedTemplate:
          errorMessage: 'not implemented yet'
