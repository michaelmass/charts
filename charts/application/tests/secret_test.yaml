suite: test secret
templates:
  - 'secret.yaml'
tests:
  - it: should fail with not implemented yet
    set:
      secret:
        enabled: true
    asserts:
      - failedTemplate:
          errorMessage: 'not implemented yet'
