suite: test networkpolicy
templates:
  - 'networkpolicy.yaml'
tests:
  - it: should fail with not implemented yet
    set:
      networkpolicy:
        enabled: true
    asserts:
      - failedTemplate:
          errorMessage: 'not implemented yet'
