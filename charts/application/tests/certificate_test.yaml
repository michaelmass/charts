suite: test certificate
templates:
  - 'certificate.yaml'
tests:
  - it: should fail with not implemented yet
    set:
      certificate:
        enabled: true
    asserts:
      - failedTemplate:
          errorMessage: 'not implemented yet'
