suite: test statefulset
templates:
  - 'statefulset.yaml'
tests:
  - it: should fail with not implemented yet
    set:
      statefulset:
        enabled: true
    asserts:
      - failedTemplate:
          errorMessage: 'not implemented yet'
